# ==== better-auth ====
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=replace-me-with-a-strong-random-secret
BETTER_AUTH_URL=http://localhost:3000

# ==== OAuth providers ====
# GitHub: https://github.com/settings/developers → New OAuth App
#   Homepage URL:       http://localhost:3000
#   Callback URL:       http://localhost:3000/api/auth/callback/github
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Google: https://console.cloud.google.com/apis/credentials
#   Authorized redirect: http://localhost:3000/api/auth/callback/google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ==== Benchmark backends (FastAPI) ====
# Fallback test backend (used only if a team has not set its own VM URL).
BACKEND_TEST_URL=http://backend:8000

# Secret expected by the global backend's /submit_final endpoint.
# Must match SUBMIT_FINAL_SECRET on the FastAPI side.
BACKEND_GLOBAL_SECRET=change-me

# ==== GPU pool (RunPod) ====
# API key for the RunPod account that owns the GPU pool.
RUNPOD_API_KEY=

# Number of warm GPU pods to keep in the pool (manual autoscaling).
RUNPOD_POOL_SIZE=1

# Worker image (built from worker/Dockerfile and pushed to a registry).
RUNPOD_POOL_IMAGE=nisseya/hack-apr-worker:latest

# GPU type for pool pods.
RUNPOD_POOL_GPU=A100-80GB

# Persistent volume size (GB) shared by pool pods for model/dep caching.
RUNPOD_POOL_VOLUME_SIZE_GB=100

services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: polars_bench
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d polars_bench"]
      interval: 3s
      timeout: 3s
      retries: 20

  backend:
    build:
      context: ./backend
    restart: unless-stopped
    environment:
      WORKSPACE_ROOT: /workspace
    volumes:
      - workspace:/workspace
    ports:
      - "8000:8000"

  web:
    build:
      context: ./frontend
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://postgres:postgres@db:5432/polars_bench
      BETTER_AUTH_SECRET: ${BETTER_AUTH_SECRET}
      BETTER_AUTH_URL: ${BETTER_AUTH_URL:-http://localhost:3000}
      GITHUB_CLIENT_ID: ${GITHUB_CLIENT_ID}
      GITHUB_CLIENT_SECRET: ${GITHUB_CLIENT_SECRET}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET}
      BACKEND_TEST_URL: ${BACKEND_TEST_URL:-http://backend:8000}
      BACKEND_GLOBAL_SECRET: ${BACKEND_GLOBAL_SECRET:-}
    depends_on:
      db:
        condition: service_healthy
      backend:
        condition: service_started
    ports:
      - "3000:3000"
    command: sh -c "npm run db:migrate && npm run start"

volumes:
  pgdata:
  workspace:
